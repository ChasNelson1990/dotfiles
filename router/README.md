# Router

Status: **Planned** — hardware not yet chosen.

The network edge (Box 1) — a dedicated firewall/router appliance, kept separate from the compute
core so routing keeps working through a crash, reboot, or troubleshooting session on the AI/NAS
box. See the [top-level README](../README.md) for how this fits alongside the other devices.

Full plan lands in a follow-up PR.
